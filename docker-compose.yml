services:
  portfolio:
    build: .
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Origin CA cert + key live on the host (gitignored), mounted read-only.
      - ./certs:/etc/nginx/certs:ro
